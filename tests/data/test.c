#include <stdio.h>

int main() {
  // This is an example C file for line length testing.
  printf("Hello, world!\n");
  
  // The next line is intentionally longer than 78 characters to serve as an example of an overly long line.
  printf("This is a C file example with a very, very, very, very, very, very, very long line.\n");
  
  int x = 10;
  int y = 20;
  
  // Another long line for demonstration purposes
  printf("The sum of %d and %d is %d\n", x, y, x+y);
  
  return 0;
}

// More lines here...
// ...
// The end.